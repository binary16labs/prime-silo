node_modules/
dist/
packaging/node_modules/
packaging/runtime-bundle/
runtime/
.benny_home/
home/
# Static marketing site — hand-authored/templated HTML Prettier's strict parser rejects.
site/
# YAML is parsed by the app's custom restrictive yaml-lite parser, which rejects
# Prettier's flow-style/indentation output (e.g. commands/params.yaml). Leave all
# YAML untouched.
**/*.yaml
**/*.yml
**/*.min.js
**/vendor/

# Ignore benchmark history and generated test prompts
tests/agent_llm_performance/results/
tests/agent_llm_performance/prompts/

# Generated by scripts/manual_build.mjs from manual/manual.json. The generator owns their
# shape, and reformatting them here would simply be undone by the next build — a permanent
# dirty-tree loop. manual/manual.json is hand-edited source and stays checked.
manual/OPERATING-MANUAL.md
manual/tour.html
manual/manual.rag.md
manual/manual.pageindex.md
manual/manual.rag.jsonl
manual/manual.triples.json

# QLoRA training outputs and eval dumps — model checkpoints, tokenizers and generated
# result files. Machine-written artifacts of a training run, not source: formatting them
# achieves nothing, and a tokenizer.json is a data file whose shape belongs to the library
# that wrote it.
scripts/train/qlora/out_*/
scripts/train/eval/out_*/
manual/SITEMAP.md
manual/sitemap.json
