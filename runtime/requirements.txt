fastapi>=0.115.0
uvicorn>=0.34.0
httpx>=0.28.0
pydantic>=2.10.0
litellm>=1.60.0
langchain>=0.3.0
langgraph>=0.2.0
chromadb>=0.5.0
openlineage-python>=1.27.0
arize-phoenix>=5.0.0
python-multipart>=0.0.20
pypdf>=5.0.0
pathspec>=0.12.0
tiktoken>=0.7.0
rich>=13.0.0
neo4j>=5.25.0
networkx>=3.4.0
# Extra dependencies for SDLC and Pypes
polars>=1.43.2
pyarrow>=25.0.1
jsonschema>=4.26.0
# Optional: HNSW approximate-nearest-neighbour index for semantic_correlate.
hnswlib>=0.8.0
# MCP support
mcp>=1.0
# Document parsing for RAG ingest (HTML cleanup + PDF text). Imported by
# benny/api/file_routes.py (bs4, markdownify, fitz==PyMuPDF) — required for the
# Documents surface; previously undeclared, which broke the bundled API import.
beautifulsoup4>=4.15.0
markdownify>=1.2.3
PyMuPDF>=1.28.2
# Optional rich DocModel backend (VIS-001 / ADR-003). The DEFAULT backend is
# PyMuPDF (above) — lean, offline, no torch. Docling gives higher-accuracy
# layout/table structure but pulls a torch/transformers stack and fetches HF
# model weights on first use, so it is NOT in the shipped bundle. Install to opt
# into backend="docling". Pillow is only needed by the Docling backend's crop step.
# docling>=2.4.0
# Pillow>=10.0.0
# Code graph (Tree-Sitter AST + language grammars). Imported by
# benny/graph/code_analyzer.py — required for the Code 3D surface.
tree-sitter>=0.26.0
tree-sitter-python>=0.25.0
tree-sitter-javascript>=0.25.0
tree-sitter-typescript>=0.23.2
# Dev/Test
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-repeat>=0.9.0
ruff>=0.8.0
black>=24.0.0
