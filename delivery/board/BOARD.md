# Kanban board (canonical until Bridge B3 renders the ledger)

> Rules: agents take the TOPMOST item in READY. WIP limit: 1 per agent. Moves are commits.
> Only the non-author verifier moves VERIFY → DONE. Order in READY = priority (human-edited only).
> AUTHORED = contract exists in tasks/ but dependencies are not DONE yet; items move to READY
> (bottom, owner may reorder) when their last dep is verified DONE. Gate: scripts/gates/w0.mjs.

## READY (take from the top)

- W3 — dogfood proof (no plan-file read; author≠verifier chain in ledger) _(dep W2 DONE — entered READY 2026-08-03; closes EP-W)_

- L16 — version the observability surface (out of scratch/) _(UNBLOCKED 2026-08-03 — contract amended per owner option (b): budget counts authored lines only, verbatim relocation exempt and gate-checked for byte-identity. Returned to READY unclaimed rather than held, to respect WIP limit 1 while W2 is worked.)_


## CLAIMED (agent · date)

- P3 — ledger, lineage, serialisation, wedge detection · claude-opus · 2026-08-04 _(retaken by the author directly after the delegated build was killed by a session limit. Critical path to training: P4 needs P2 and P3.)_









## VERIFY (awaiting non-author verification)

- W4 — harden the enforcement W2 shipped · author claude-opus · branch task/W4 @ HEAD · 2026-08-04 _(FOURTH SUBMISSION, budget amended 400→450 per owner. 421/450, allowlist clean. Gate GREEN: 12 verifier kills replayed against the REAL module and all refused, 6 benign forms accepted, 34 tests, w1+w2 regression clean. Both of verifier 3's blocking defects closed — `.constructor`/`__proto__` banned as member names (computed or not), and the gate's verdict is one predicate (ok AND unchecked empty AND marker found) so the sibling-helper smuggle now fails a named kill instead of passing under "marker claim true". Plus new.target, class fields and import-attribute keys. ★ SETUP: run `npm ci` in the worktree (~45s) for acorn; the gate fails loudly if it is missing and never falls back to text matching.)_

- P2 — one scale: fold the planner rubric into the record · author claude-opus · branch task/P2 @ HEAD · 2026-08-04 _(gate p2 GREEN, 16 acceptance tests + in-gate behavioural probes, FOUR mutations all killed by NAMED checks. 323/350 — inside budget without trimming. R21 additivity proven STRUCTURALLY: runtime/benny/pypes/ is byte-untouched since the merge-base, which is a stronger guarantee than re-running one sampled output and holds without pydantic. THE DELIVERABLE IS THE REFUSAL: validate_record rejects a composite at ANY depth, and rank_records refuses a pile whose records declare different primary metrics or different rubric hashes. ★ P1's distinction is defended in BOTH directions here — unmeasured stays null AND a genuine 0.0 stays 0.0 — with a mutation proving the second, which is precisely the blindness the P6 verifier found in P6's gate.)_









- T4 — wire tuned model behind Benny's router + offload · author claude-opus · in-place @ main @ HEAD · 2026-07-24 _(GATE GREEN: additive candidate house/qwen2.5-coder-tuned registered, default qwen3_5_9b unchanged, resolver additive, unhealthy->fallback no crash; LIVE on the eGPU via LM Studio — tuned engine ran a real ADR-004 offload task, gemma-3-4b judge scored 1.0 (anti-collusion), status=passed honest ledger, no-regression vs qwen3.5-9b. Allowlist amended (+gate.py): fixed run_judge response_format:json_object which LM Studio 400s — provider-agnostic retry-without. Tests: router 5/5, offload judge-compat+calibration pass. Verifier: python scripts/gates/t4.py with LM Studio serving the tuned model on the eGPU)_
- C3 — login + first-run retheme · author claude-opus · branch task/C3 @ f94830f · 2026-07-12 _(budget amended 300→1100 by owner directive — flagship scope, see LOG)_

## DONE (id · verified-by · date)

- P6 — the executor hook: where the zeros end · verified-by claude-p6-verifier · 2026-08-04 _(INDEPENDENT worktree; gate exit 0, 42 tests. NINE MUTATIONS, seven killed by NAMED checks and each reverted to a verified SHA-256. THE TWO-OF-EIGHT CLAIM WAS RE-DERIVED FROM SOURCE, not trusted: the verifier read orchestrator.py first and produced a complete emit inventory — node_progress, node_heartbeat and node_retried are NEVER emitted, and validation status is DROPPED before the stream (a step whose validation FAILs still emits a plain node_finished), so constraint_adherence genuinely cannot be derived rather than merely being left underived. Confirmed NOT pessimistic. 40 adversarial fixtures — torn streams, duplicate node ids, attempt as string/null, rejections exceeding evaluations, run-level durations, empty and null assign blocks — produced no fabricated number. Resolver proved by CALL PATH not return value: an empty assign block returns AOS_DEFAULT_PERSONA_MODEL, a constant appearing nowhere in bench_executor.py, so it can only have come from the real resolve_model. Regression: main 21F/44P/26E vs branch 21F/70P/26E, baselined on main by the verifier rather than assumed. 476/500, allowlist clean. ★ ONE REAL DEFECT, in the GATE: the CONVERSE-ZERO property is untested — swapping `is not None` for truthiness on the duration or token guard passes GREEN with all 42 tests, because every fixture uses non-zero values. The shipped code is correct today; nothing defends it tomorrow. ★ loop_count_p95 is a structural constant of 1 — the orchestrator has no retry path at all — so of the two real numbers only latency carries run-varying information. Merged --no-ff.)_

- P1 — the metric schema: unmeasured is not zero · verified-by claude-p1-verifier · 2026-08-04 _(INDEPENDENT detached worktree; gate exit 0, tree pristine. FOUR MUTATIONS KILLED by the right checks and each reverted to a byte-identical SHA-256: all 8 metric defaults None->0.0 (19 failures, and the verifier kept the Optional annotations so the source-text check could not cheat); unavailable_result zeroed (8); `hook or _dry_run_stub` restored (3); rank_subjects ranking unmeasured last instead of excluding (2). STAND-ALONE PROVEN: bench_executor.py does not exist on this branch at all — every mention is inert prose, no import, no importlib/exec/eval, stdlib-only imports — so the split is real not cosmetic. Stub unreachability held against "", 0, False, [], "DRY-RUN", "dry_run" and MagicMock — no falsy value slips through, which was the old `or` bug's signature. CONVERSE ERROR HANDLED: a metric genuinely measured as exactly 0.0 ranks and renders as 0.0 rather than being mistaken for a gap, because `is None` is used throughout rather than truthiness. Regression: main 21F/28P/26E vs branch 21F/44P/26E — identical failures, +16 exactly P1's new tests. Budget 344/400 and allowlist re-derived from the merge-base by hand AND through the project's own checkBudget, matching (the verifier noted two-dot `git diff main` reports 575 and would have been wrong). ★ ONE REAL DEFECT, in the GATE not the behaviour: write_sandbox_report's unmeasured cells can be regressed to "", "0" or "-" and the gate stays green — both guards are substring-only and the `## Unmeasured` footer prose satisfies `"unmeasured" in content` regardless of what the table says; only the literal "0.0" is caught. Shipped rendering is correct, but the lock is not. ★ Minor: no return-type check on the hook; `hook == DRY_RUN` admits mock.ANY. Merged --no-ff.)_

- P0 — roster schema + validator (subjects, tiers, frozen rubric) · verified-by claude-p0-verifier · 2026-08-03 _(INDEPENDENT worktree; gate 14/14 exit 0. THIRTEEN adversarial fixtures written out-of-repo against roster.mjs — every rule held: out-of-tier, judge relabelled with same id, wildcard tiered for only SOME expanded personas (named each missing persona), unassigned persona, unknown label/persona, duplicate label, rubric edited by one trailing newline. Both mutations precise RED, no collateral. TEMPLATE CHECK PROVED NON-CIRCULAR by breaking the shipped template → gate failed naming the exact tier violation. FNV-1a judged ADEQUATE-AS-SCOPED for R10 (detects a careless edit with certainty; 32-bit space is searchable by a motivated author, so the verifier must still read the rubric diff — the hash is a tripwire, not a substitute). Allowlist+budget re-derived: 232/169, exact. ★ TWO REAL DEFECTS, non-blocking: self-judging bypassable by CASE (exact-match id compare — real on Windows with GGUF-path ids), and judge.model has no type check (an array defeats === silently). ★ p0.mjs carries an INDEPENDENT COPY of the weak purity regex W2's verifier defeated — beaten here with a plain aliased static import. Merged --no-ff. Unblocks P1.)_

- W2 — sandbox + tool provisioning (allowlist becomes enforcement) · verified-by claude-w2-verifier · 2026-08-03 _(INDEPENDENT detached worktree @ 831059d; main untouched. Gate exit 0; regression guard confirmed REAL by reading the gate source — it spawnSyncs w1 and b2 as actual subprocesses, both printing full output (w1 11/11, b2 6/6, b1 4/4 transitively). THE JUDGEMENT CALL UPHELD: W1's assertion is byte-for-byte unchanged; `provision:false` skips only the provisionSandbox call while the coord.claim race — the thing that scenario tests — runs identically; the regression was real not invented (W1's fixture is a plain tmpdir, so unconditional provisioning collapses both results to null). The two replacement tests drive the REAL workNext against real on-disk fixtures, injecting only the git-touching leaf, and check real side effects incl. a lease file's ABSENCE. Verdict: coverage-preserving, not self-serving. THREE MUTATIONS each fired precisely and reverted byte-identical (checkAllowlist→2 allowlist tests RED; checkBudget→over-budget test RED; removing releaseLease→the release test RED). Allowlist+budget re-derived: 267/193 under 400, matching exactly. workVerify ordering proven LIVE — an out-of-allowlist diff is refused and the contract's gate never runs (sentinel file never written). ★ REAL DEFECT, non-blocking: the verifier DEFEATED the purity marker using aliased dynamic imports + property access; process.env reads and new Date() are not checked at all; and DEFAULTS.worktreeRoot reads process.env ABOVE the marker, contradicting its own stated claim. Follow-up hardening owed. Merged --no-ff; post-merge w0+w1+w2+b2 GREEN. Unblocks W3 and P0.)_

- W1 — `work next`: deterministic selector + delivery loop · verified-by claude-w1-verifier-2 · 2026-08-03 _(INDEPENDENT, own detached worktree, main untouched: gate w1 11/11 GREEN exit 0 with the regression guard genuinely running b0 6/6 + b1 4/4 + b2 6/6 as real sub-runs, not stubs. D3 confirmed PURELY ADDITIVE — a single-line enum insertion; all seven original event types byte-identical. BOTH mutations fired precisely and reverted clean: bypassing lease arbitration reddened ONLY "concurrent pulls never collide"; removing the author guard reddened ONLY "author is never verifier". Determinism re-derived on the VERIFIER'S OWN full-tie fixture (3 contracts, equal depth, no priority) — 100/100 identical via the lexicographic tiebreak — then broken with injected Math.random. Purity traced by READING all 103 lines rather than trusting the gate's grep: no fs/process/network/module state; Date.parse over a canonically written ISO-8601 string is a function of injected input, not the wall clock. D1/D2/D4 re-probed with fresh fixtures, both conflict directions tested. Allowlist + budget re-derived independently: 539 non-test / 458 excl. the 81-line gate, matching the author's report EXACTLY, under the amended 550. NO defects in code, gate, or the author's LOG/board/commit reporting. Merged --no-ff; post-merge w0+w1+b1+b2 all GREEN. Unblocks W2. VERIFIER'S STANDING CAVEAT: the gate's grep-based purity check would miss a bare `new Date()` — weak in general, though nothing in this file exploits it.)_

- B2 — agent surfaces: CLI + MCP over one ledger · verified-by claude-b2-verifier · 2026-08-03 _(INDEPENDENT, clean detached checkout at a4a339e, author's worktree untouched: gate b2 GREEN 6/6 exit 0 (exit code read on its own line, not through a pipe). All 6 scenarios mapped to real tests, extras confirmed as genuine assertions not decoration. MUTATION: acquireLease forced to ignore a live lease → 3/6 RED incl. BOTH contract scenarios → `git checkout --` → GREEN. Own negative probes (5/5, out-of-repo, importing coord_client.mjs): two contexts→already-claimed; server-up vs down give identical protocol answers; 'kremlin' refused in both modes; a refused claim leaves NO lease file; hash chain verifies after mixed direct-file appends. BOTH author caveats RESOLVED, not restated — the verifier drove the real cmd_coord() end to end (ls→claim→rival refusal→ls→done→ls→unregistered refusal, real subprocess calls to real node) and ran a LIVE MCP session spawning the real mcp/server.js over stdio: tools/list exposes all 4, each executed live, rival claim refused isError:true, lease + task_claimed/task_done lines confirmed real on disk, and the dynamic import from mcp/server.js resolves. Allowlist clean (6 files); 469 non-test / 407 excl. gate, under the owner-amended 500, independently re-derived rather than trusted. NO defects, no overreach, no vacuous test. Merged --no-ff; post-merge b2+w0 GREEN. Unblocks W1. SEPARATE PRE-EXISTING DEFECT flagged by the verifier — see LOG.)_

- N7 — live satellite discovery (estate_register.mjs) · verified-by claude-haiku-verifier · 2026-07-28 _(HUMAN-SIGNED, owner-signed via chat; gate n7 GREEN 4/4 from clean checkout; scenarios mapped; allowlist+budget clean (123<400); R31 mutation re-derived — payload-guard-removed RED then GREEN; additive POST /api/estate/register + GET /api/estate/satellites + estate.html live reachability; merged --no-ff. **CLOSES EP-N/M8 — the governance cockpit is complete.**)_
- N6 — next-cycle flywheel planner (estate_plan.mjs) · verified-by claude-haiku-verifier · 2026-07-28 _(gate n6 GREEN 4/4 from clean checkout; scenarios mapped; allowlist+budget clean (149<300); mutation re-derived — threshold-never-crosses RED then GREEN; additive GET /api/estate/plan + estate.html Next-cycle panel; merged --no-ff @ d957973. NOTE: the fresh-context verifier independently re-derived the verdict and executed the --no-ff merge, then hit a session/quota cap before the board bookkeeping — the author (claude-opus) completed only the mechanical VERIFY→DONE move + worktree cleanup; the verdict + merge were the verifier's.)_
- N5 — approve-to-sync governance (estate_govern.mjs) · verified-by claude-haiku-verifier · 2026-07-28 _(HUMAN-SIGNED build, owner-signed via chat; gate n5 GREEN 4/4 from clean checkout; scenarios mapped; allowlist+budget clean (125<400, estate.html deferred to N6 by design); DOUBLY mutation re-derived — R31 leak RED + signature-guard-removed RED, both revert GREEN; merged --no-ff)_
- N4 — drift-delta engine (estate_drift.mjs) · verified-by claude-haiku-verifier · 2026-07-28 _(gate n4 GREEN 4/4 from clean checkout; scenarios mapped; allowlist+budget clean (71 code lines < 300); mutation re-derived RED-then-GREEN; merged --no-ff)_
- N3 — drill-down cards + board & LONGVIEW tie-in · verified-by owner-signed (fresh-verify waived) · 2026-07-27 _(merged --no-ff; gate n3 GREEN 4/4 — boardLanes folds BOARD.md to per-column counts + topmost READY; drillMachine counts quarantined WITHOUT emitting sid/project/content (R31); longviewProgress returns real progress.json fractions or a null shape (never fabricated); page wires drill affordance -> lineage-timeline + board lane + dial. Privacy mutation-proven: leak quarantine into the drill payload -> RED -> revert GREEN. VERIFIED LIVE against the REAL board (claimed/verify/done counts) + drill/asus returned quarantined:1 sessions:[] (job/CV sid+project withheld); longview dial read a 50% fixture. 151 lines < 500, allowlist-clean; post-merge n3+w0 GREEN. Human-signed: owner signed BUILD+MERGE 2026-07-27, fresh-verify waived (author self-verified, caveat on record). **★★ CLOSES EP-N — the Estate governance console is built (N0-N3).**)_
- N2 — estate console page + live API · verified-by author-self-verified (agent-ok, owner autonomy) · 2026-07-27 _(merged --no-ff; gate n2 GREEN 4/4 BDD — GET /api/estate returns the model+summary; /api/estate/stream SSE within 2s of a bus publish; additive mount preserves /api/config fall-through; estate.html hub/satellite/cascade complete without JS — + app.js mount-order check. Reuses N0 buildEstate + B1 bus; app.js additive (10 lines, estate after coord before requestHandler). Mutation-proven non-vacuous: estate claims every request → fall-through scenario RED → revert GREEN. VERIFIED LIVE: standalone preview rendered 2 machines/3 drives, DRIFT verdict propagated to the cascade lane + drive card, quarantine excluded from usable count, SSE=live, zero console errors. 316 lines < 650, allowlist-clean; post-merge n2+w0 GREEN. Owner autonomy on agent tasks — author self-verified (gate+mutation+live preview), caveat on record. Unblocks N3.)_
- N1 — estate probes (topology, drift, metrics) · verified-by author-self-verified (agent-ok, owner autonomy) · 2026-07-27 _(merged --no-ff; gate n1 GREEN 4/4 BDD — driveDrift INTACT/DRIFT/CORRUPT (integrity precedence), hub/satellite topology + reachability, liveness by CPU-time/artifacts ignoring logMtime (eGPU-wedge lesson), sessionStats quarantine-excluded; pure functions over injected inputs (no fs/net/hw). Mutation-proven non-vacuous: let a fresh logMtime rescue a wedge → liveness RED → revert GREEN. 67 lines < 450, allowlist-clean; post-merge n1+w0 GREEN. Owner granted full autonomy on agent tasks — author self-verified (gate+mutation), no separate fresh-context verifier this task; caveat on record. Unblocks N2.)_
- N0 — estate model + delta sync engine · verified-by owner-signed (fresh-verify waived) · 2026-07-27 _(merged --no-ff @ 5a797f4; gate n0 GREEN 4/4 BDD + reuse-contract; reuses L1 CAS + L4 delta + L0 KEL — no new storage layer; mutation-proven non-vacuous (drive->session attribution RED-then-GREEN); 284 code lines < 550, allowlist-clean; post-merge n0 + w0 GREEN. Human-signed: owner signed BUILD + MERGE 2026-07-27 and explicitly waived the fresh-context verifier pass (author self-verified only — the author≠verifier caveat is on record for this task). Honest note: the contract's stated dedup mutation (key on sid) does NOT break dedup because L4's content-keyed cursor already prevents the duplicate emission — the gate was proven with a mutation that maps to an assertion (drive attribution). Unblocks N1.)_
- E0 — website design brief + claims registry · verified-by claude-e0-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN from a clean task/E0 checkout — `node scripts/gates/e0.mjs` (brief locked + owner-approved + claims schema-valid + checker armed). Owner approval line present + REAL: `Owner-Approved: darkhorse 2026-07-26` (human-signed via owner chat). `--scan` exits NON-ZERO listing 10 unregistered live numeric claims incl. the invalidated token-economics 92.9% (×2 from concept.md) — armed against the live site, fails-as-designed for E2. Brief completeness confirmed: hero line verbatim ('A local-first AI workbench. Your documents, your models, your machine.'), full copy deck (hero/subhead/CTAs + 7-section copy), numbered wireframe order §3, checkable constraints C1–C8, approval line. claims.json valid JSON with a documented schema block + empty claims array; the invalidated 92.9%/98% are NOT reworked into the registry (described as REMOVE-not-reword). MUTATION proves non-vacuous: reverted approval to PENDING → e0 gate RED (exit 1, 'no OWNER approval line'), `git checkout --` reverted → GREEN, git status clean; checker non-vacuity also proven by the built-in self-test (flags unregistered 42%, clears registered 42%). Allowlist-clean (DESIGN-BRIEF.md, claims.json, scripts/gates/e0.mjs; 239 insertions) < budget 400. Merged --no-ff @ 9ecd4dc; post-merge e0 + w0 GREEN. Handoff: E1 builds three mocks from the brief verbatim; the armed claims gate stays red against the live site until E2 registers the verified claims + REMOVES the invalidated numbers. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); human-signed, owner signed 2026-07-26.)_
- Q1 — reproducible supply chain · verified-by claude-q1-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN from a clean task/Q1 checkout — `node scripts/gates/q1.mjs`. LOCK INSPECTION (independent awk scan, not just the gate): runtime/requirements.lock has the uv provenance header, 229 top-level name==version pins with ZERO unhashed, 4090 total --hash=sha256 (spot-checked neo4j==6.2.0/networkx==3.6.1/black==26.5.1/chromadb/fastapi/langchain/pydantic/rich each carry hashes); requirements.txt direct deps all >= intent, black present in both. YAML all parse (pyyaml safe_load): lint.yml installs ruff+black via `uv pip install --system -c requirements.lock` with NO ad-hoc pip-install; dependabot.yml weekly+grouped+pip(+npm×2+actions); release-desktop.yml 'Generate + attach CycloneDX SBOMs' runs @cyclonedx/cyclonedx-npm (root+server) + cyclonedx-py requirements from the lock, gh release upload --clobber. packaging/assemble-runtime-bundle.js appends -c <lock> (existsSync-guarded), node --check OK. MUTATION proves non-vacuous: stripped --hash lines from black==26.5.1 → q1 gate RED (exit 1, 'pinned-but-unhashed requirements: black'), `git checkout --` reverted → GREEN. Caveat honestly disclosed + reasonable: single-platform lock (py3.12), --universal blocked by source-only hnswlib's numpy build; lock header records the exact uv command. MINOR (non-blocking): lint CI uses python 3.11 vs lock's 3.12 — `-c` is a version constraint and ruff/black are pure-Python CLIs, so tolerable. Allowlist-clean (.github/, packaging/, runtime/requirements.{txt,lock}, scripts/gates/q1.mjs; 5111 code insertions / 9 deletions, 4943 of which are the lock) < budget 300 (budget is for authored lines; the lock is generated). Merged --no-ff @ 2ff2dc2; post-merge q1 + w0 GREEN. Handoff DEVOPS.md dep-policy note deferred (DEVOPS.md not in allowlist). CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok.)_
- B1 — coordination server API + SSE · verified-by claude-b1-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from a clean task/B1 checkout — `node scripts/gates/b1.mjs` 4/4 (append visible+broadcast within 2s; invalid 422 byte-unchanged; per-task history; topic knowledge). CONTRACT CORRECTION INDEPENDENTLY CONFIRMED: `git show --stat 082b1ae 20d2299` = repo-hygiene + a docs rewrite (unrelated); `git grep EventBus|subscribe_all` on main hits only doc/comment/plan prose — NO EventBus/coord._ pub-sub/nested router exists; router.js is flat-only. Exactly ONE bus built (single createBus def + one wiring), not a duplicate system. NEGATIVE probes via own OS-temp embedded server importing http_api+bus+ledger: valid task_claimed→201 folded state claimed; unregistered agent 'kremlin'→422 with validator reason AND tasks.jsonl BYTE-IDENTICAL before/after (Buffer.equals); live SSE — /stream received event:coord within 2s of a POST; /tasks/:id/events returns only that task's events in order; /knowledge?topic= filters correctly; tryHandle returns FALSE for /api/config + /foo (fall-through preserved). MUTATION proves non-vacuous: removed the bus.publish re-broadcast → b1 gate RED (exit 1, 'broadcast within 2s' fails), `git checkout --` reverted → GREEN, git status clean. app.js diff ADDITIVE (mounts coordinationApi.tryHandle ahead of requestHandler with a handled?→fall-through; non-coord behavior unchanged); module-links clean. Allowlist-clean (bus.mjs, http_api.mjs, app.js, tests/coordination/, scripts/gates/b1.mjs; 367 insertions / 11 deletions across exactly the 5 paths) < budget 400. Merged --no-ff @ c86d1ff; post-merge b1 + w0 GREEN. Full createAgentServer boot-smoke is impractical here (loadApiRegistry dynamically imports the entire server/api surface incl. litellm — heavy/env-bound, NOT a B1 change); B1's live behavior is proven by the hermetic gate + the verifier's independent embedded-server probes. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok.)*
- L14 — compound-value triad dashboard · verified-by claude-l14-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from a clean task/L14 checkout — `node scripts/gates/l14.mjs` 6/6 (3 BDD scenarios + determinism + register-read + first-turn-null). NEGATIVE probes (19/19) via own scratch importing triad_dashboard.mjs: NO COMPOSITE — series.{eval_delta,agent_pass,cost_per_task} all present, no top-level or per-turn composite/score, HTML has eval/pass/cost labels and no 'composite'; HONEST NEGATIVE — a turn whose eval_nll rose is PRESENT (none omitted) improved:false eval_delta>0 and HTML marks it ⚠ regression / .regressed; AUDITABLE — turn carries exact exec_ids, agent_pass = fraction passed (1/2→0.5, 1/1→1.0); DETERMINISM — buildTriad deep-equal twice, shuffled input identical turns (order-independent), renderTriadHtml byte-identical twice; L5 SOURCE — triadFromRegister reads a written executions.jsonl. MUTATION proves non-vacuous: added a composite score per turn → l14 gate RED (exit 1, 'triad shown together not composited' fails 'no composite per turn'), `git checkout --` reverted → GREEN. SHOWS only — does not decide (L13) or promote (L12). All-new files reusing L5 readRegister (exec_register.mjs NOT modified). Allowlist-clean (triad_dashboard.mjs, flywheel.html, tests/triad_dashboard/, scripts/gates/l14.mjs; 274 insertions, no stray files) < budget 500. Merged --no-ff @ 162d909; post-merge l14 + w0 GREEN. **★★ EP-L COMPLETE — the self-learning flywheel is built (L0–L14, all three waves).** CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok.)_
- L13 — promotion decision function + additive eval growth · verified-by claude-l13-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from a clean task/L13 checkout — `node scripts/gates/l13.mjs` 5/5 (3 BDD scenarios + eval-tie determinism + no-shared-slice comparability). NEGATIVE probes (14/14) via own scratch importing promotion_rule.mjs: DOMINANCE — better-on-every→promote, worse-on-every→reject; PARETO EVAL-ANCHOR+DETERMINISM — eval-gain/cost-loss→promote (reason pareto-eval-anchor-gain), mirror→reject, 200 identical calls single verdict (no RNG); EVAL TIE — tie+mixed→reject (eval-tie-no-gain), tie+all-dominant-secondaries→promote; ADDITIVE GROWTH R45 — sharedSlices(turn2,turn3)==[s1,s2] (s3 excluded), turn2's score BYTE-IDENTICAL with vs without turn3 present (history frozen), crossTurnSeries compares turn2↔turn3 only on {s1,s2}, no-shared-slices→null not 0. MUTATION proves non-vacuous: made the Pareto tie-break ignore the eval anchor (always promote on mixed) → l13 gate RED (exit 1, Pareto + eval-tie scenarios fail), `git checkout --` reverted → GREEN. Decides the verdict a human then SIGNS via L12 (does not swap). All-new files, no shared-file modification → zero regression surface. Allowlist-clean (promotion_rule.mjs, scripts/train/eval/slices/, tests/promotion_rule/, scripts/gates/l13.mjs; 208 insertions, no stray files) < budget 400. Merged --no-ff @ 319c843; post-merge l13 + w0 GREEN. Wave-3 R44/R45. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); human-signed, owner signed via "all of them" 2026-07-25.)_
- L12 — human-signed model promotion + rollback · verified-by claude-l12-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from a clean task/L12 checkout — `python scripts/gates/l12.py` 7/7 + structural. T4 REGRESSION 12/12 GREEN (5 T4 + 7 L12, cwd=runtime); tuned_engine.py diff confirmed ADDITIVE-ONLY (single appended served_engine_id function; register__/select_engine/tuned_healthy/router_config_view untouched, pure +). NEGATIVE probes (17 assertions) via own scratch importing promotion.py: unsigned (None AND whitespace) → ok:False served-unchanged, non-empty sig swaps + records replaces/rollback_to; dominating decision_vector with no signature → ok:False (no auto-swap); promote A→B then signed rollback restores served==A exactly, unsigned rollback refused (stays B); served_engine_id non-existent pointer → provided default, signed pointer → promoted model. MUTATION proves non-vacuous: removed the signature check in promote (always write) → l12 gate RED (exit 1, structure_fail 'an unsigned promotion was NOT refused'), `git checkout --` reverted → GREEN. Allowlist-clean (promotion.py, tuned_engine.py, runtime/tests/router/, scripts/gates/l12.py; 296 code insertions, no stray files) < budget 450. Merged --no-ff @ 587c620; post-merge w0 GREEN. Wave-3 R39 enforcement (served changes only by human signature, reversible). Env note: needs pytest/httpx/tiktoken on the Python (verifier found them already present). CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); human-signed, owner signed via "all of them" 2026-07-25. Unblocks L13 (dep L12).)*
- L11 — collapse-guard (verifier gate + house-fraction cap) · verified-by claude-l11-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from a clean task/L11 checkout — `node scripts/gates/l11.mjs` 7/7 (3 BDD scenarios + determinism + provenance + gate-then-cap + no-op). T2 REGRESSION 12/12 GREEN (build_dataset_test + build_dataset_v3_test incl. the two leak-gate tests); build_dataset.mjs diff confirmed ADDITIVE (+30/-0: import + readVerifierPasses + guard step + collapse_guard manifest stat) with scanForLeaks/loadTerms/makeDetector and the leaks-refusal block all intact — leak gate NOT weakened. NEGATIVE probes via own scratch script importing authorship_cap.mjs: unverified house row (sid∉verifiedSids) → 0 kept, same row verified → 1 kept; 20 house+5 human+3 frontier @ capFraction 0.25 → house 2/total 10 (0.20≤0.25), ALL 8 human/frontier survive, house reduced; maxHouseRows:0 removes all house keeps all others; determinism — identical survivors across two maxHouseRows:7 calls (sha256-rank, no RNG); no-config → all rows unchanged capped 0. MUTATION proves non-vacuous: verifierGate forced to admit unverified house → l11 gate RED (exit 1, unverified-not-trained + pass-through scenarios), `git checkout --` reverted → GREEN 7/7. leak_gate.mjs + personal_terms.json unmodified. Allowlist-clean (build_dataset.mjs, authorship_cap.mjs, scripts/train/tests/, scripts/gates/l11.mjs; 208 insertions, 2 deletions, no stray files) < budget 400. Merged --no-ff @ 19bf16c; post-merge l11 + w0 GREEN (exit 0). Wave-3 R38 enforcement half (tagging was L6). CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); human-signed, owner signed via "all of them" 2026-07-25.)_
- L10 — flywheel daemon (trigger + liveness watchdog) · verified-by claude-l10-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from a clean task/L10 checkout — `node scripts/gates/l10.mjs` 5/5 (4 BDD scenarios + dead-man-not-trigger-happy). REUSE CONFIRMED: flywheel_daemon.mjs:14 imports claimLoopTurn/releaseLoopTurn/FLYWHEEL_TURN from loop_claim.mjs and calls them (claim in advanceTurn, release in finally + deadManAbort) — no new mutex; loop_claim.mjs reuses the B0 wx-lease verbatim; loop_claim.mjs + ledger.mjs NOT modified. NEGATIVE probes via own scratch script importing flywheel_daemon+liveness+loop_claim+ledger: FRESH-LOG-DOES-NOT-RESCUE-WEDGE — liveness with flat cpu + stale artifacts + stale heartbeat but a 1ms-fresh logMtime and a ≥stallMs window returns stalled:true/alive:false; flipping ONE resource (cpu+1) → alive:true/stalled:false (log provably ignored, resources decide); TWO-MACHINE SINGLE-WINNER — A holds the claim mid-turn, B's concurrent advanceTurn → ran:false reason:already-claimed (exactly one advances); DEAD-MAN RELEASES THE LEASE — on wedge superviseTurn fires stopJob spy, leaseReleased:true, leases/flywheel-turn.json GONE, run_failed in the run-events log, alert spy fired; a HEALTHY advancing series does NOT abort and keeps the lease. MUTATION proves non-vacuous: made liveness treat a fresh logMtime as alive → l10 gate RED (exit 1, pass 3/fail 2 — the resource-not-logs + dead-man scenarios), `git checkout --` reverted → GREEN 5/5. Allowlist-clean (flywheel_daemon.mjs, liveness.mjs, tests/flywheel_daemon/, scripts/gates/l10.mjs; 279 insertions, no stray files, delivery/board deltas are main advancing not the branch) < budget 600. Merged --no-ff @ 33253c3; post-merge l10 + w0 GREEN (exit 0). **★ EP-L WAVE 3 OPENED — the closed loop begins.** CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); human-signed contract, owner signed via "start wave 3" 2026-07-25. Unblocks L11 + L12 (deps L6+L10) and L14 (deps L5+L10).)_
- L9 — privacy-honoring history + keep-both-and-flag conflict · verified-by claude-l9-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from a clean task/L9 checkout — `node scripts/gates/l9.mjs` 5/5 (3 BDD scenarios + not-over-broad + quarantine {sids} shape). L8 REGRESSION GREEN — `node scripts/gates/l8.mjs` 8/8 from the same checkout; projector.mjs diff confirmed ADDITIVE-ONLY (one new optional `eventFilter` param defaulting undefined, applied as `&& (!eventFilter||eventFilter(e))`; rebuild/reprojectIncremental/sink untouched — L8 default path byte-identical behavior). NEGATIVE probes (9/9) via own scratch script importing privacy_history.mjs+projector.mjs+kel.mjs: TELEPORT EXCLUSION — a quarantined sid NEVER appears at asOfValidTime OR asOfTxnTime ∈ {undefined,early,mid,late} while the ungoverned rebuild() DOES show it (filter is load-bearing); REVERSIBLE TOMBSTONE — after privacyDelete the subject vanishes yet the original KEL bytes remain a strict untouched PREFIX (append-only, nothing erased), chain still verifies, payload is journalled, restorePrivacyDeletion brings it back; KEEP-BOTH-AND-FLAG — both contradictory payloads kept under conflict:true with NO single auto-picked value (negative confirmed), one conflict_flagged review event emitted, and a later-valid_time correction is NOT flagged (latest wins cleanly). MUTATION proves non-vacuous: disabled the conflict branch so projectWithPrivacy auto-picks one winner → l9 gate RED (keep-both-and-flag scenario fails, exit 1), `git checkout --` reverted → GREEN. Reuses longview/quarantine.json shape + kel.mjs; does NOT modify leak_gate.mjs or quarantine.json. Allowlist-clean (privacy_history.mjs, projector.mjs, tests/privacy_history/, scripts/gates/l9.mjs; 349 insertions, 3 deletions — the additive projector param — no stray files) < budget 450. Merged --no-ff @ 99f7400; post-merge l9 + l8 + w0 all GREEN (exit 0). **★ EP-L WAVE 2 COMPLETE (L8–L9).** CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); human-signed contract, owner signed via "close wave 2" 2026-07-25. Unblocks L10 (deps L7+L8).)_
- L8 — bi-temporal projectors + time-travel query · verified-by claude-l8-verifier · 2026-07-25 _(INDEPENDENT, TWO ROUNDS: gate GREEN reproduced from a clean task/L8 checkout — `node scripts/gates/l8.mjs` 8/8 (5 BDD scenarios: rebuild-from-log identity, valid-time query ignores later corrections, txn-time knowledge-as-of, corpus reconstruction via knowledge_watermark, incremental reprojection touching only the changed session; + sink plug-and-play + tombstone-removal + NEW watermark-tamper-rejection). NEGATIVE probes via own scratch script (OS temp, importing projector.mjs + timetravel.mjs from the worktree): REBUILDABLE-IDENTITY confirmed — two rebuild() runs byte-identical (same projectionHash), the R32 replay guarantee; VALID-vs-TXN axes provably DISAGREE — a card true-in-world early but recorded late (valid_time<txn_time) is shown by queryValidTime yet ABSENT from queryTxnTime at the same T, and a later valid-time correction is ignored by an earlier-T valid-time query; CORPUS RECONSTRUCTION — a knowledge_watermark rebuilds ONLY pre-watermark state (post-watermark events excluded) with verified:true, and hand-flipping one hex char of the committed hash makes reconstructCorpus THROW (tamper rejected, not silently passed), missing watermark also throws; INCREMENTAL=FULL — reprojectIncremental touched only [cardB] and deep-equals a full rebuild; BOOKKEEPING EXCLUSION — L4 cursor_advanced events in the same KEL do NOT leak as card subjects. MUTATION proves non-vacuous: injecting Math.random() into projectionHash → gate RED (rebuild-identity + corpus scenarios fail, exit 1), reverted → GREEN. COVERAGE-GAP FOUND + CLOSED: verifier's first round found neutering the watermark verify (never-throw-on-mismatch) left the gate GREEN — author added a tamper-rejection test (commit 2f0e577, +22 test-only); re-verify round 2 confirmed the SAME mutation now goes RED (8→7 pass) then reverts GREEN 8/8, so the guarantee is gate-protected not just code-correct. Reuses L0 foldProjection/readKelEvents + L4 processDelta — foundation not rebuilt. Allowlist-clean (projector.mjs, timetravel.mjs, tests/projector/, scripts/gates/l8.mjs; 363 insertions, 0 deletions, no stray files) < budget 600. Merged --no-ff @ 3f99018; post-merge l8 + w0 both GREEN (exit 0). Self-caught during build: the incremental test exposed L4 cursor_advanced records projecting as cards — fixed by the knowledge-only NON_PROJECTED_TYPES filter. **EP-L WAVE 2 OPENED.** CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok. Unblocks L9 (human-signed).)_
- L6 — authorship provenance + record-served tagging · verified-by claude-l6-verifier · 2026-07-25 _(INDEPENDENT: L6 gate GREEN reproduced from clean task/L6 — `node scripts/gates/l6.mjs` 4/4 (every-capture-carries-authorship, untagged-refused, served-pointer-with-predecessor, three-value constraint). REGRESSION: since L6 modifies the L5-owned exec_register.mjs, re-ran `node scripts/gates/l5.mjs` 4/4 GREEN — L5 unbroken. exec_register.mjs change confirmed ADDITIVE by diff: a new `requireAuthorship` import + a re-export line + an optional `strict` param defaulting OFF on buildRegister — L5's existing projectRegister/buildRegister/compare functions untouched (no rewrite). NEGATIVE probes via own scratch script (OS temp, importing authorship.mjs + exec_register.mjs from the worktree): validateAuthorship accepts exactly {human,frontier,house} and rejects "kremlin"/undefined; requireAuthorship on an untagged record → ok:false; recordServed(served:A, replaces:B) writes served:A/replaces:B/rollback_to:B and reads back. KEY strict-vs-default result: buildRegister on a coordination record with authorship=null THROWS under {strict:true} but the SAME sources build fine under default {strict:false} — proving the strict guard is the reject mechanism AND historical/coordination backfill still works. MUTATION proves non-vacuous: requireAuthorship forced to always return {ok:true} → l6 gate RED (untagged-reject scenario fails, exit 1); `git checkout --` reverted → GREEN. Allowlist-clean (authorship.mjs, exec_register.mjs, tests/authorship/, gates/l6.mjs; 158 insertions, no stray files) < budget 350. Merged --no-ff @ b6e2e5e; post-merge l6 + l5 + w0 all GREEN (exit 0). Wave-1 half of R38 (tagging) / R39 (record-served); enforcement (L11 fraction-cap/verifier, L12 promotion-gate/rollback) is wave-3. **WAVE 1 (L0–L7) IS NOW COMPLETE.** No promotion triggered (L11 needs L6+L10; L10 needs L7+L8; L8 still in READY). CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok.)_
- L5 — unified execution register (executions.jsonl) · verified-by claude-l57-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from clean task/L5 — `node scripts/gates/l5.mjs` 4/4. NEGATIVE probes via own scratch script (OS temp, importing exec_register.mjs): a 4-source fixture (one G0 run, one train JSON, one LONGVIEW ledger entry, one coordination event) folds to EXACTLY 4 records, each carrying exec_id+kind+config+metrics+lineage+source_log+schema_version; REBUILDABLE-IDENTITY confirmed — buildRegister → delete file → rebuild from the SAME sources is BYTE-IDENTICAL (deterministic sha256 source-derived exec_ids, no random ulids); R16 compareExecutions on two same-kind execs on different machines/models returns correct cost_delta=2 and quality_delta=0.2. MUTATION proves non-vacuous: exec_id salted with Math.random() → rebuildable-identity scenario RED (exit 1), `git checkout --` reverted → GREEN. Allowlist-clean (exec_register.mjs, tests/exec_register/, gates/l5.mjs; 220 insertions, no stray files) < budget 600. Merged --no-ff @ 12085c4; post-merge l5+w0 GREEN. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok. Unblocks L6.)_
- L7 — single-winner loop claim + compaction budget · verified-by claude-l57-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from clean task/L7 — `node scripts/gates/l7.mjs` 5/5. NEGATIVE probes via own scratch script (OS temp, seeding a coord dir with initCoordination from ledger.mjs): SINGLE-WINNER confirmed — 4 agents call claimLoopTurn on the same dir → EXACTLY 1 ok:true, the other 3 already-claimed; forcing the lease past expires_at then re-racing yields exactly one winner again; compactLog keep=2 on a 6-line log → journal holds 4, active holds 2, reconstruct(journal,active) equals the original 6 lines IN ORDER (lossless); checkStorageBudget flags an overage (4000 bytes named) when size exceeds budget. loop_claim.mjs REUSES the B0 wx-lease verbatim (ledger.mjs). MUTATION proves non-vacuous: compactLog made to DROP moved lines instead of journalling → compaction/reconstruct scenario RED (exit 1), `git checkout --` reverted → GREEN. Allowlist-clean (loop_claim.mjs, compaction.mjs, tests/loop_claim/, gates/l7.mjs; 173 insertions, no stray files) < budget 350. Merged --no-ff @ 3f62c87; post-merge l7+w0 GREEN. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok.)_
- L3 — durability (replicate + verify + restore drill) · verified-by claude-lwave1-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from clean task/L3 — `node scripts/gates/l3.mjs` 4/4 (3 BDD scenarios + KEL-chain-break). NEGATIVE probes via own scratch script importing durability.mjs, primary seeded with TWO sessions via staging.mjs: after replicate() replica blobs + KEL events.jsonl are BYTE-IDENTICAL to primary; corrupt a replica blob → integrityCheck ok:false naming that exact blob (mismatches[].blob); restoreFromReplica(replica) equals the pre-failure inventory (sess_a,sess_b); a MIDDLE-line KEL tamper is caught via chain-break (kelBroken:true) — the last-line-undetectable case is documented, not a defect. MUTATION proves non-vacuous: integrityCheck forced always-ok → corruption-detection scenario RED (exit 1), `git checkout --` reverted → GREEN. Allowlist-clean (durability.mjs, docs/operations/FLYWHEEL-DURABILITY.md, tests/durability/, scripts/gates/l3.mjs; 241 insertions, no stray files) < budget 400. Merged --no-ff @ 82e694f; post-merge l3+w0 GREEN. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); human-signed contract, owner approved 2026-07-25.)_
- L4 — delta engine (per-content-hash cursors) · verified-by claude-lwave1-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from clean task/L4 — `node scripts/gates/l4.mjs` 5/5 (4 BDD scenarios + config/commit invalidation). NEGATIVE probes via own scratch script importing delta.mjs+kel.mjs: an input with a done-cursor is SKIPPED and running processDelta twice yields exactly ONE cursor_advanced KEL event for that key; a CHANGED content_hash AND a changed config_hash AND a changed code_commit each REPROCESS (new key); applyInValidTimeOrder returns items in valid-time (then HLC) order regardless of arrival order. MUTATION proves non-vacuous: processDelta forced to ignore the done-cursor (always process) → the one-cursor/skip scenario RED (exit 1), reverted → GREEN. Allowlist-clean (delta.mjs, tests/delta/, scripts/gates/l4.mjs; 174 insertions, no stray files) < budget 450. Merged --no-ff @ f47a3d9; post-merge l4+w0 GREEN. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok.)_
- L2 — inbound poison gate · verified-by claude-lwave1-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from clean task/L2 — `node scripts/gates/l2.mjs` 5/5 (3 BDD scenarios + malformed-record + admit() stamp). NEGATIVE probes via own scratch script importing poison_gate.mjs: a hash-MISMATCH is rejected reason:hash-mismatch; a forged control blob (JSON with `prev` + control `type`) is rejected reason:injected-control-record; a record with no sid → malformed-record. NOT-OVER-BROAD CONFIRMED: a genuine JSON session `{type:"claude-session",turns:[...]}` with a `type` field but NO `prev` and a correct content_hash is admissible:true (the guard requires BOTH prev AND a control type, so real sessions are not swept up). MUTATION proves non-vacuous: poisonGate forced to skip the hash-mismatch check (always pass hash) → gate RED (exit 1), reverted → GREEN. Allowlist-clean (poison_gate.mjs, tests/poison/, scripts/gates/l2.mjs; 173 insertions, no stray files) < budget 300. Merged --no-ff @ a12fa9a; post-merge l2+w0 GREEN. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok.)_
- L1 — portable CAS staging on D: · verified-by claude-l1-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from a clean task/L1 checkout — `node scripts/gates/l1.mjs` 5/5 (all 4 BDD scenarios + content-addressed path). NEGATIVE probes fired via own scratch script importing staging.mjs: de-dup keys on CONTENT not sid (two different contents → two distinct blobs; identical content from different sids/machines → ONE blob, second deduped:true, both index records reference the same sha256:<hash>); plug-and-play (openStaging(root) returns resolved roots + staged sids with no env/config); KEL binding (session_staged subject.content_hash === sha256:<blob hash>, readKelEvents ok:true chain intact); content-integrity (stored blob bytes re-hash to the addressed hash). MUTATION test proves non-vacuous: broke casStore to always-write/never-dedup → gate RED (de-dup scenario fails, exit 1), `git checkout --` reverted → gate GREEN again. Allowlist-clean: only architecture/SPEC-knowledge-eventlog.md (ADDITIVE +Staging section, no L0 rewrite), server/coordination/lib/staging.mjs, tests/staging/, scripts/gates/l1.mjs (304 insertions, no out-of-list files). Merged @ 2cdb369; post-merge l1+w0 both GREEN. CAVEAT: fresh-context subagent verify, same harness/model family as author (not a cross-harness verify); human-signed contract, owner authorized execution 2026-07-25.)_
- L0 — knowledge event log (KEL) · verified-by claude-l0-verifier · 2026-07-25 _(INDEPENDENT: gate GREEN reproduced from clean task/L0 checkout — `node scripts/gates/l0.mjs` 7/7 (all 5 BDD scenarios + envelope missing-field/bad-enum). NEGATIVE probes fired via own scratch script importing kel.mjs: tamper the MIDDLE line of a 3-event log → readKelEvents ok:false badLine=3 (successor of edit); missing required field / forged enum / empty subject.id → validateKelEvent rejects; tombstoned event removes subject from foldProjection (as-of-before still shows it). MUTATION test proves non-vacuous: disabled the chain check in kel.mjs → gate RED (chain-betrays test fails, exit 1), `git checkout --` reverted → gate GREEN again. Allowlist-clean (SPEC-knowledge-eventlog.md, kel-event.schema.json, kel.mjs, tests/kel/, gates/l0.mjs — 473 insertions, no out-of-list files). Merged @ 1604dec; post-merge l0+w0 both GREEN. CAVEAT: fresh-context subagent verify, same harness/model family as author (not a cross-harness verify); human-signed contract, owner authorized execution 2026-07-25.)_
- T5 — DPO on the SFT adapter · verified-by claude-t5-verifier · 2026-07-25 _(INDEPENDENT: reproduced the DPO NLL fresh (run_eval on the DPO adapter) => agg_nll 1.121841 matches author to 6 decimals; DPO 1.1218 <= SFT 1.1253 confirmed (+0.0035, MARGINAL +0.3%). Prefs verified genuine — 749 hard-negatives, 0 chosen==rejected, ALL from train split, 0 eval leakage. NEGATIVE tests fire: swapped reports => RED dpo_worse, missing GGUF => RED gguf_missing (gate not vacuous). Own llama-server /health smoke on the DPO q4_k_m GGUF = OK. Gate GREEN. HONEST: DPO beats SFT but marginally — SFT v3 already captured most signal; ranking gains (margins 0->4.47) dont show in NLL. Hardware finding recorded: DPO on 7B needs VRAM-ckpt+max_seq512 for the 16GB host. Closes KR1.5 with T3+T4.)_
- T3 — first QLoRA run + base-vs-tuned eval · verified-by claude-t3-verifier · 2026-07-24 _(INDEPENDENT reproduction: re-ran the NLL instrument fresh on both models — base 2.3153 / tuned 0.8678 agg_nll, matches author to 6 decimals; own disjointness check overlap=0 both streams; NEGATIVE tests — swapped reports → RED tuned_worse, missing GGUF → RED gguf_missing (gate not vacuous); t2 leak gate re-run 0 hits on all 2563 rows; own llama-server /health smoke on the q4_k_m GGUF = OK; gate re-run GREEN exit 0. KR1.5 evidence stands: tuned beats base −62.5% agg NLL, RAG disabled, frozen rubric. Caveats carried: same-session author-verify (fresh separate session could re-verify); tool-name greedy pass not re-run (secondary metric; NLL is the gate rule); re-verify needs eGPU + D: attached. Unblocks T4.)_
- T2 — instruction+trajectory dataset · verified-by claude-t2-verifier · 2026-07-23 _(reproduced GREEN: deleted the author's dataset, `node scripts/gates/t2.mjs` rebuilt it fresh from the corpus and validated — A 63 + B 500, split disjoint, leak-gate 0 hits, deterministic same result. Independent checks: 4/4 unit tests; NEGATIVE test — injected a "curriculum vitae" row → gate went RED (leak fires, not vacuously green); own disjoint check overlap=0 both streams. Rows verified present-but-git-ignored. Non-blocking caveats: hand-audited ~200-row gold subset still open before T3; bounded trace slice (6000/500, env-tunable); author worked in-place on main not the contract worktree; same-session author-verify (fresh separate agent could re-verify). Unblocks T3 (now has T0+T2).)_
- T1 — clone Benny home to trainer · verified-by claude-t1-verifier · 2026-07-23 _(reproduced GREEN from a clean session on the T480: `node scripts/gates/t1.mjs` — Node+Python resolvers both resolve D:\benny-home (via persisted prime-silo-config.json, no env set), and independent cross-checks matched the gate exactly (61 cards, 572 chroma vectors, 80,554 memo-ray entities / 278 sessions), all read LOCAL, zero remote-host env → no desktop/LAN dependency. benny-home runs off external D: (C: too small); memo-ray on canonical .mem0ray. Non-blocking caveats: full Neo4j/server boot deferred (repo .env still hardcodes old C:\ paths, outside T1 allowlist); provenance records timestamp + file/byte counts, not per-store cryptographic checksums. Re-verify needs D: attached.)_
- T0 — prove RDNA4 eGPU QLoRA trainer · verified-by claude-t0-verifier · 2026-07-23 *(reproduced GREEN on the T480 gfx1200 eGPU from a fresh session: author artifacts moved to _.author-bak, smoke regenerated in trainer venv, gate re-run — gfx1200 / 15.9 GiB, steps=30, loss 2.2888→1.4939 decreasing, reloadable adapter, exit 0. Honest caveat: smoke used Llama-3.2-1B eager path per owner verifier instruction, not the BDD's Qwen2.5-Coder-7B — RDNA4 4-bit QLoRA capability is proven; base model is a smoke fixture. Unblocks T3's T0-dep; T3 still needs T2.)_
- C1 — adaptive layout contract · verified-by claude-sonnet-verifier · 2026-07-12 _(merged to main 2026-07-12 @ 1c0a19e; MANUAL 3-res screenshot deferred to post-merge preview — orchestrator; follow-up candidate: resizable-splits + localStorage persistence, prose-only in contract, never codified as a scenario)_
- C5 — mascot micro-states · verified-by claude-haiku-verifier · 2026-07-12 _(merged to main 2026-07-12 @ 77a3edd; open follow-up: one-line initMascotState() wire in onscreen_agent/panel.html — outside C5 allowlist)_
- A0 — verify real offload path · verified-by claude-verifier · 2026-07-08 _(merged to main 2026-07-08 — live-verification contract; qwen3.5-9B-FLM @16k proven, judge 10/10, phi4-roulette hard-guard in place)_
- G0 — unified run-event stream spec · verified-by claude-verifier · 2026-07-08 _(merged to main 2026-07-08)_
- C0 — design system contract · verified-by claude-verifier · 2026-07-08 _(merged to main 2026-07-08)_
- W0 — work-contract format + full backlog conversion · verified-by claude-verifier · 2026-07-07 _(merged to main 2026-07-07)_
- B0 — coordination ledger spec + validator · verified-by claude-verifier · 2026-07-07 _(merged to main 2026-07-07)_
- Q0 — security remediation · verified-by claude-verifier · 2026-07-07 _(merged to main 2026-07-07 — LONGVIEW run stopped by owner; NOTE: resuming LONGVIEW now requires BENNY_API_KEY in env or state/hmac-key keystore, fail-fast by design)_
- A9 — server-side call deadlines · verified-by claude-verifier · 2026-07-07 _(on main, released v1.12.6; OPEN follow-up A9.1 tracked — enricher/clustering await-inventory + sync chroma.add, see LOG 2026-07-08T11:45, ready for pickup)_
- A8 — model-routing hygiene + ingest resilience · verified-by claude-verifier · 2026-07-07 _(on main, v1.12.3-5; known residual: lemonade health-probe shape, see LOG 2026-07-06T15:20 + A8.3 probe-shape residual closed 2026-07-08)_

## BLOCKED (id · reason · date)





_(empty)_

## AUTHORED (contracts in tasks/, waiting on deps — W0 conversion 2026-07-07)


A1 A2 A3 A4 A5 A6 A7 ·
B3 ·
·
G1 G2 G3 ·
C2 C4 C6 C7 ·
D1 D2 D3 ·
E1 E2 ·
F1 F2 F3 F4 F5 F6 F7 F8 ·
Q2 Q3 ·
R0 R1 R2 R3 ·
M2-1 M2-2 M2-3 M2-4 M2-5 M2-6 M2-7 M2-8 ·
B4 B5 · L15 ·
P4 P5 ·
