# Kanban board (canonical until Bridge B3 renders the ledger)

> Rules: agents take the TOPMOST item in READY. WIP limit: 1 per agent. Moves are commits.
> Only the non-author verifier moves VERIFY → DONE. Order in READY = priority (human-edited only).
> AUTHORED = contract exists in tasks/ but dependencies are not DONE yet; items move to READY
> (bottom, owner may reorder) when their last dep is verified DONE. Gate: scripts/gates/w0.mjs.

## READY  (take from the top)
- B1 — coordination server API + SSE  *(dep B0 DONE — entered READY 2026-07-07)*
- Q1 — reproducible supply chain  *(dep Q0 DONE — entered READY 2026-07-07)*
- E0 — website design brief + claims registry  *(zero-dep — entered READY 2026-07-07; human-signed)*
## CLAIMED (agent · date)
- L9 — privacy-honoring history + keep-both-and-flag conflict · claude-opus · 2026-07-25 *(human-signed: owner directive "close wave 2" 2026-07-25)*

## VERIFY (awaiting non-author verification)
- T4 — wire tuned model behind Benny's router + offload · author claude-opus · in-place @ main @ HEAD · 2026-07-24 *(GATE GREEN: additive candidate house/qwen2.5-coder-tuned registered, default qwen3_5_9b unchanged, resolver additive, unhealthy->fallback no crash; LIVE on the eGPU via LM Studio — tuned engine ran a real ADR-004 offload task, gemma-3-4b judge scored 1.0 (anti-collusion), status=passed honest ledger, no-regression vs qwen3.5-9b. Allowlist amended (+gate.py): fixed run_judge response_format:json_object which LM Studio 400s — provider-agnostic retry-without. Tests: router 5/5, offload judge-compat+calibration pass. Verifier: python scripts/gates/t4.py with LM Studio serving the tuned model on the eGPU)*
- C3 — login + first-run retheme · author claude-opus · branch task/C3 @ f94830f · 2026-07-12 *(budget amended 300→1100 by owner directive — flagship scope, see LOG)*

## DONE (id · verified-by · date)
- L8 — bi-temporal projectors + time-travel query · verified-by claude-l8-verifier · 2026-07-25 *(INDEPENDENT, TWO ROUNDS: gate GREEN reproduced from a clean task/L8 checkout — `node scripts/gates/l8.mjs` 8/8 (5 BDD scenarios: rebuild-from-log identity, valid-time query ignores later corrections, txn-time knowledge-as-of, corpus reconstruction via knowledge_watermark, incremental reprojection touching only the changed session; + sink plug-and-play + tombstone-removal + NEW watermark-tamper-rejection). NEGATIVE probes via own scratch script (OS temp, importing projector.mjs + timetravel.mjs from the worktree): REBUILDABLE-IDENTITY confirmed — two rebuild() runs byte-identical (same projectionHash), the R32 replay guarantee; VALID-vs-TXN axes provably DISAGREE — a card true-in-world early but recorded late (valid_time<txn_time) is shown by queryValidTime yet ABSENT from queryTxnTime at the same T, and a later valid-time correction is ignored by an earlier-T valid-time query; CORPUS RECONSTRUCTION — a knowledge_watermark rebuilds ONLY pre-watermark state (post-watermark events excluded) with verified:true, and hand-flipping one hex char of the committed hash makes reconstructCorpus THROW (tamper rejected, not silently passed), missing watermark also throws; INCREMENTAL=FULL — reprojectIncremental touched only [cardB] and deep-equals a full rebuild; BOOKKEEPING EXCLUSION — L4 cursor_advanced events in the same KEL do NOT leak as card subjects. MUTATION proves non-vacuous: injecting Math.random() into projectionHash → gate RED (rebuild-identity + corpus scenarios fail, exit 1), reverted → GREEN. COVERAGE-GAP FOUND + CLOSED: verifier's first round found neutering the watermark verify (never-throw-on-mismatch) left the gate GREEN — author added a tamper-rejection test (commit 2f0e577, +22 test-only); re-verify round 2 confirmed the SAME mutation now goes RED (8→7 pass) then reverts GREEN 8/8, so the guarantee is gate-protected not just code-correct. Reuses L0 foldProjection/readKelEvents + L4 processDelta — foundation not rebuilt. Allowlist-clean (projector.mjs, timetravel.mjs, tests/projector/, scripts/gates/l8.mjs; 363 insertions, 0 deletions, no stray files) < budget 600. Merged --no-ff @ 3f99018; post-merge l8 + w0 both GREEN (exit 0). Self-caught during build: the incremental test exposed L4 cursor_advanced records projecting as cards — fixed by the knowledge-only NON_PROJECTED_TYPES filter. **EP-L WAVE 2 OPENED.** CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok. Unblocks L9 (human-signed).)*
- L6 — authorship provenance + record-served tagging · verified-by claude-l6-verifier · 2026-07-25 *(INDEPENDENT: L6 gate GREEN reproduced from clean task/L6 — `node scripts/gates/l6.mjs` 4/4 (every-capture-carries-authorship, untagged-refused, served-pointer-with-predecessor, three-value constraint). REGRESSION: since L6 modifies the L5-owned exec_register.mjs, re-ran `node scripts/gates/l5.mjs` 4/4 GREEN — L5 unbroken. exec_register.mjs change confirmed ADDITIVE by diff: a new `requireAuthorship` import + a re-export line + an optional `strict` param defaulting OFF on buildRegister — L5's existing projectRegister/buildRegister/compare functions untouched (no rewrite). NEGATIVE probes via own scratch script (OS temp, importing authorship.mjs + exec_register.mjs from the worktree): validateAuthorship accepts exactly {human,frontier,house} and rejects "kremlin"/undefined; requireAuthorship on an untagged record → ok:false; recordServed(served:A, replaces:B) writes served:A/replaces:B/rollback_to:B and reads back. KEY strict-vs-default result: buildRegister on a coordination record with authorship=null THROWS under {strict:true} but the SAME sources build fine under default {strict:false} — proving the strict guard is the reject mechanism AND historical/coordination backfill still works. MUTATION proves non-vacuous: requireAuthorship forced to always return {ok:true} → l6 gate RED (untagged-reject scenario fails, exit 1); `git checkout --` reverted → GREEN. Allowlist-clean (authorship.mjs, exec_register.mjs, tests/authorship/, gates/l6.mjs; 158 insertions, no stray files) < budget 350. Merged --no-ff @ b6e2e5e; post-merge l6 + l5 + w0 all GREEN (exit 0). Wave-1 half of R38 (tagging) / R39 (record-served); enforcement (L11 fraction-cap/verifier, L12 promotion-gate/rollback) is wave-3. **WAVE 1 (L0–L7) IS NOW COMPLETE.** No promotion triggered (L11 needs L6+L10; L10 needs L7+L8; L8 still in READY). CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok.)*
- L5 — unified execution register (executions.jsonl) · verified-by claude-l57-verifier · 2026-07-25 *(INDEPENDENT: gate GREEN reproduced from clean task/L5 — `node scripts/gates/l5.mjs` 4/4. NEGATIVE probes via own scratch script (OS temp, importing exec_register.mjs): a 4-source fixture (one G0 run, one train JSON, one LONGVIEW ledger entry, one coordination event) folds to EXACTLY 4 records, each carrying exec_id+kind+config+metrics+lineage+source_log+schema_version; REBUILDABLE-IDENTITY confirmed — buildRegister → delete file → rebuild from the SAME sources is BYTE-IDENTICAL (deterministic sha256 source-derived exec_ids, no random ulids); R16 compareExecutions on two same-kind execs on different machines/models returns correct cost_delta=2 and quality_delta=0.2. MUTATION proves non-vacuous: exec_id salted with Math.random() → rebuildable-identity scenario RED (exit 1), `git checkout --` reverted → GREEN. Allowlist-clean (exec_register.mjs, tests/exec_register/, gates/l5.mjs; 220 insertions, no stray files) < budget 600. Merged --no-ff @ 12085c4; post-merge l5+w0 GREEN. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok. Unblocks L6.)*
- L7 — single-winner loop claim + compaction budget · verified-by claude-l57-verifier · 2026-07-25 *(INDEPENDENT: gate GREEN reproduced from clean task/L7 — `node scripts/gates/l7.mjs` 5/5. NEGATIVE probes via own scratch script (OS temp, seeding a coord dir with initCoordination from ledger.mjs): SINGLE-WINNER confirmed — 4 agents call claimLoopTurn on the same dir → EXACTLY 1 ok:true, the other 3 already-claimed; forcing the lease past expires_at then re-racing yields exactly one winner again; compactLog keep=2 on a 6-line log → journal holds 4, active holds 2, reconstruct(journal,active) equals the original 6 lines IN ORDER (lossless); checkStorageBudget flags an overage (4000 bytes named) when size exceeds budget. loop_claim.mjs REUSES the B0 wx-lease verbatim (ledger.mjs). MUTATION proves non-vacuous: compactLog made to DROP moved lines instead of journalling → compaction/reconstruct scenario RED (exit 1), `git checkout --` reverted → GREEN. Allowlist-clean (loop_claim.mjs, compaction.mjs, tests/loop_claim/, gates/l7.mjs; 173 insertions, no stray files) < budget 350. Merged --no-ff @ 3f62c87; post-merge l7+w0 GREEN. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok.)*
- L3 — durability (replicate + verify + restore drill) · verified-by claude-lwave1-verifier · 2026-07-25 *(INDEPENDENT: gate GREEN reproduced from clean task/L3 — `node scripts/gates/l3.mjs` 4/4 (3 BDD scenarios + KEL-chain-break). NEGATIVE probes via own scratch script importing durability.mjs, primary seeded with TWO sessions via staging.mjs: after replicate() replica blobs + KEL events.jsonl are BYTE-IDENTICAL to primary; corrupt a replica blob → integrityCheck ok:false naming that exact blob (mismatches[].blob); restoreFromReplica(replica) equals the pre-failure inventory (sess_a,sess_b); a MIDDLE-line KEL tamper is caught via chain-break (kelBroken:true) — the last-line-undetectable case is documented, not a defect. MUTATION proves non-vacuous: integrityCheck forced always-ok → corruption-detection scenario RED (exit 1), `git checkout --` reverted → GREEN. Allowlist-clean (durability.mjs, docs/operations/FLYWHEEL-DURABILITY.md, tests/durability/, scripts/gates/l3.mjs; 241 insertions, no stray files) < budget 400. Merged --no-ff @ 82e694f; post-merge l3+w0 GREEN. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); human-signed contract, owner approved 2026-07-25.)*
- L4 — delta engine (per-content-hash cursors) · verified-by claude-lwave1-verifier · 2026-07-25 *(INDEPENDENT: gate GREEN reproduced from clean task/L4 — `node scripts/gates/l4.mjs` 5/5 (4 BDD scenarios + config/commit invalidation). NEGATIVE probes via own scratch script importing delta.mjs+kel.mjs: an input with a done-cursor is SKIPPED and running processDelta twice yields exactly ONE cursor_advanced KEL event for that key; a CHANGED content_hash AND a changed config_hash AND a changed code_commit each REPROCESS (new key); applyInValidTimeOrder returns items in valid-time (then HLC) order regardless of arrival order. MUTATION proves non-vacuous: processDelta forced to ignore the done-cursor (always process) → the one-cursor/skip scenario RED (exit 1), reverted → GREEN. Allowlist-clean (delta.mjs, tests/delta/, scripts/gates/l4.mjs; 174 insertions, no stray files) < budget 450. Merged --no-ff @ f47a3d9; post-merge l4+w0 GREEN. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok.)*
- L2 — inbound poison gate · verified-by claude-lwave1-verifier · 2026-07-25 *(INDEPENDENT: gate GREEN reproduced from clean task/L2 — `node scripts/gates/l2.mjs` 5/5 (3 BDD scenarios + malformed-record + admit() stamp). NEGATIVE probes via own scratch script importing poison_gate.mjs: a hash-MISMATCH is rejected reason:hash-mismatch; a forged control blob (JSON with `prev` + control `type`) is rejected reason:injected-control-record; a record with no sid → malformed-record. NOT-OVER-BROAD CONFIRMED: a genuine JSON session `{type:"claude-session",turns:[...]}` with a `type` field but NO `prev` and a correct content_hash is admissible:true (the guard requires BOTH prev AND a control type, so real sessions are not swept up). MUTATION proves non-vacuous: poisonGate forced to skip the hash-mismatch check (always pass hash) → gate RED (exit 1), reverted → GREEN. Allowlist-clean (poison_gate.mjs, tests/poison/, scripts/gates/l2.mjs; 173 insertions, no stray files) < budget 300. Merged --no-ff @ a12fa9a; post-merge l2+w0 GREEN. CAVEAT: fresh-context subagent, same harness/model family as author (not cross-harness); agent-ok.)*
- L1 — portable CAS staging on D: · verified-by claude-l1-verifier · 2026-07-25 *(INDEPENDENT: gate GREEN reproduced from a clean task/L1 checkout — `node scripts/gates/l1.mjs` 5/5 (all 4 BDD scenarios + content-addressed path). NEGATIVE probes fired via own scratch script importing staging.mjs: de-dup keys on CONTENT not sid (two different contents → two distinct blobs; identical content from different sids/machines → ONE blob, second deduped:true, both index records reference the same sha256:<hash>); plug-and-play (openStaging(root) returns resolved roots + staged sids with no env/config); KEL binding (session_staged subject.content_hash === sha256:<blob hash>, readKelEvents ok:true chain intact); content-integrity (stored blob bytes re-hash to the addressed hash). MUTATION test proves non-vacuous: broke casStore to always-write/never-dedup → gate RED (de-dup scenario fails, exit 1), `git checkout --` reverted → gate GREEN again. Allowlist-clean: only architecture/SPEC-knowledge-eventlog.md (ADDITIVE +Staging section, no L0 rewrite), server/coordination/lib/staging.mjs, tests/staging/, scripts/gates/l1.mjs (304 insertions, no out-of-list files). Merged @ 2cdb369; post-merge l1+w0 both GREEN. CAVEAT: fresh-context subagent verify, same harness/model family as author (not a cross-harness verify); human-signed contract, owner authorized execution 2026-07-25.)*
- L0 — knowledge event log (KEL) · verified-by claude-l0-verifier · 2026-07-25 *(INDEPENDENT: gate GREEN reproduced from clean task/L0 checkout — `node scripts/gates/l0.mjs` 7/7 (all 5 BDD scenarios + envelope missing-field/bad-enum). NEGATIVE probes fired via own scratch script importing kel.mjs: tamper the MIDDLE line of a 3-event log → readKelEvents ok:false badLine=3 (successor of edit); missing required field / forged enum / empty subject.id → validateKelEvent rejects; tombstoned event removes subject from foldProjection (as-of-before still shows it). MUTATION test proves non-vacuous: disabled the chain check in kel.mjs → gate RED (chain-betrays test fails, exit 1), `git checkout --` reverted → gate GREEN again. Allowlist-clean (SPEC-knowledge-eventlog.md, kel-event.schema.json, kel.mjs, tests/kel/, gates/l0.mjs — 473 insertions, no out-of-list files). Merged @ 1604dec; post-merge l0+w0 both GREEN. CAVEAT: fresh-context subagent verify, same harness/model family as author (not a cross-harness verify); human-signed contract, owner authorized execution 2026-07-25.)*
- T5 — DPO on the SFT adapter · verified-by claude-t5-verifier · 2026-07-25 *(INDEPENDENT: reproduced the DPO NLL fresh (run_eval on the DPO adapter) => agg_nll 1.121841 matches author to 6 decimals; DPO 1.1218 <= SFT 1.1253 confirmed (+0.0035, MARGINAL +0.3%). Prefs verified genuine — 749 hard-negatives, 0 chosen==rejected, ALL from train split, 0 eval leakage. NEGATIVE tests fire: swapped reports => RED dpo_worse, missing GGUF => RED gguf_missing (gate not vacuous). Own llama-server /health smoke on the DPO q4_k_m GGUF = OK. Gate GREEN. HONEST: DPO beats SFT but marginally — SFT v3 already captured most signal; ranking gains (margins 0->4.47) dont show in NLL. Hardware finding recorded: DPO on 7B needs VRAM-ckpt+max_seq512 for the 16GB host. Closes KR1.5 with T3+T4.)*
- T3 — first QLoRA run + base-vs-tuned eval · verified-by claude-t3-verifier · 2026-07-24 *(INDEPENDENT reproduction: re-ran the NLL instrument fresh on both models — base 2.3153 / tuned 0.8678 agg_nll, matches author to 6 decimals; own disjointness check overlap=0 both streams; NEGATIVE tests — swapped reports → RED tuned_worse, missing GGUF → RED gguf_missing (gate not vacuous); t2 leak gate re-run 0 hits on all 2563 rows; own llama-server /health smoke on the q4_k_m GGUF = OK; gate re-run GREEN exit 0. KR1.5 evidence stands: tuned beats base −62.5% agg NLL, RAG disabled, frozen rubric. Caveats carried: same-session author-verify (fresh separate session could re-verify); tool-name greedy pass not re-run (secondary metric; NLL is the gate rule); re-verify needs eGPU + D: attached. Unblocks T4.)*
- T2 — instruction+trajectory dataset · verified-by claude-t2-verifier · 2026-07-23 *(reproduced GREEN: deleted the author's dataset, `node scripts/gates/t2.mjs` rebuilt it fresh from the corpus and validated — A 63 + B 500, split disjoint, leak-gate 0 hits, deterministic same result. Independent checks: 4/4 unit tests; NEGATIVE test — injected a "curriculum vitae" row → gate went RED (leak fires, not vacuously green); own disjoint check overlap=0 both streams. Rows verified present-but-git-ignored. Non-blocking caveats: hand-audited ~200-row gold subset still open before T3; bounded trace slice (6000/500, env-tunable); author worked in-place on main not the contract worktree; same-session author-verify (fresh separate agent could re-verify). Unblocks T3 (now has T0+T2).)*
- T1 — clone Benny home to trainer · verified-by claude-t1-verifier · 2026-07-23 *(reproduced GREEN from a clean session on the T480: `node scripts/gates/t1.mjs` — Node+Python resolvers both resolve D:\benny-home (via persisted prime-silo-config.json, no env set), and independent cross-checks matched the gate exactly (61 cards, 572 chroma vectors, 80,554 memo-ray entities / 278 sessions), all read LOCAL, zero remote-host env → no desktop/LAN dependency. benny-home runs off external D: (C: too small); memo-ray on canonical .mem0ray. Non-blocking caveats: full Neo4j/server boot deferred (repo .env still hardcodes old C:\ paths, outside T1 allowlist); provenance records timestamp + file/byte counts, not per-store cryptographic checksums. Re-verify needs D: attached.)*
- T0 — prove RDNA4 eGPU QLoRA trainer · verified-by claude-t0-verifier · 2026-07-23 *(reproduced GREEN on the T480 gfx1200 eGPU from a fresh session: author artifacts moved to *.author-bak, smoke regenerated in trainer venv, gate re-run — gfx1200 / 15.9 GiB, steps=30, loss 2.2888→1.4939 decreasing, reloadable adapter, exit 0. Honest caveat: smoke used Llama-3.2-1B eager path per owner verifier instruction, not the BDD's Qwen2.5-Coder-7B — RDNA4 4-bit QLoRA capability is proven; base model is a smoke fixture. Unblocks T3's T0-dep; T3 still needs T2.)*
- C1 — adaptive layout contract · verified-by claude-sonnet-verifier · 2026-07-12 *(merged to main 2026-07-12 @ 1c0a19e; MANUAL 3-res screenshot deferred to post-merge preview — orchestrator; follow-up candidate: resizable-splits + localStorage persistence, prose-only in contract, never codified as a scenario)*
- C5 — mascot micro-states · verified-by claude-haiku-verifier · 2026-07-12 *(merged to main 2026-07-12 @ 77a3edd; open follow-up: one-line initMascotState() wire in onscreen_agent/panel.html — outside C5 allowlist)*
- A0 — verify real offload path · verified-by claude-verifier · 2026-07-08 *(merged to main 2026-07-08 — live-verification contract; qwen3.5-9B-FLM @16k proven, judge 10/10, phi4-roulette hard-guard in place)*
- G0 — unified run-event stream spec · verified-by claude-verifier · 2026-07-08 *(merged to main 2026-07-08)*
- C0 — design system contract · verified-by claude-verifier · 2026-07-08 *(merged to main 2026-07-08)*
- W0 — work-contract format + full backlog conversion · verified-by claude-verifier · 2026-07-07 *(merged to main 2026-07-07)*
- B0 — coordination ledger spec + validator · verified-by claude-verifier · 2026-07-07 *(merged to main 2026-07-07)*
- Q0 — security remediation · verified-by claude-verifier · 2026-07-07 *(merged to main 2026-07-07 — LONGVIEW run stopped by owner; NOTE: resuming LONGVIEW now requires BENNY_API_KEY in env or state/hmac-key keystore, fail-fast by design)*
- A9 — server-side call deadlines · verified-by claude-verifier · 2026-07-07 *(on main, released v1.12.6; OPEN follow-up A9.1 tracked — enricher/clustering await-inventory + sync chroma.add, see LOG 2026-07-08T11:45, ready for pickup)*
- A8 — model-routing hygiene + ingest resilience · verified-by claude-verifier · 2026-07-07 *(on main, v1.12.3-5; known residual: lemonade health-probe shape, see LOG 2026-07-06T15:20 + A8.3 probe-shape residual closed 2026-07-08)*

## BLOCKED (id · reason · date)
*(empty)*

## AUTHORED (contracts in tasks/, waiting on deps — W0 conversion 2026-07-07)
A1 A2 A3 A4 A5 A6 A7 ·
B2 B3 ·
W1 W2 W3 ·
G1 G2 G3 ·
C2 C4 C6 C7 ·
D1 D2 D3 ·
E1 E2 ·
F1 F2 F3 F4 F5 F6 F7 F8 ·
Q2 Q3 ·
R0 R1 R2 R3 ·
M2-1 M2-2 M2-3 M2-4 M2-5 M2-6 M2-7 M2-8 ·
L10 L11 L12 L13 L14 ·
