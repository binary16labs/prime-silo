# EP-R — Reverse-engineer any application

**Objective:** O4 · **Goals:** P6 · **Milestone:** M2 (spec may start in M1)
**Plan source:** `../../architecture/PLAN-local-power-unified-ui.md` (workstream R)

Point at any repo: ingestion+graph → evidence dossiers @16k → citation-grounded TOGAF SAD with deterministic diagrams → modular-service blueprint. Model narrates; extractors produce facts; every claim cites graph nodes.

## Phases → task contracts

- [ ] `R0` — target-app ingestion profile (fixture: chattabot/audio-converter-ui)
- [ ] `R1` — evidence dossiers @16k (deterministic assembly, poisoned-citation gate)
- [ ] `R2` — TOGAF SAD + Mermaid/Graphviz diagrams (endpoint set-equality gate)
- [ ] `R3` — modularization blueprint (community detection + strangler-fig; advisory only)

## Exit

All phase gates green, verified by non-author agent; close with a VISION-CHECK note (plan §0.5):
which KRs moved, measured evidence, one honest sentence on drift.
