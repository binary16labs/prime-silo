---
id: M2-6
epic: EP-B
milestone: M2
okr: O1O4.trust-wave-2
deps: [C6, B2, R0]
authority: agent-ok
allowlist: [server/coordination/, runtime/benny/, runtime/tests/, tests/coordination/, scripts/gates/m2-6.mjs]
tools: [node, python]
sandbox: worktree
verify: node scripts/gates/m2-6.mjs
budget: 400
---

# M2-6 — Institutional memory mesh (graph sync to memo-ray)

## Goal
B-workstream knowledge notes and R-workstream graph builders emit semantic triples
(`[Symbol] CALLS [Endpoint]`, `[Doc] EXPLAINS [Component]`) into memo-ray via its MCP server —
insights become permanently searchable institutional memory instead of transient workspace state.

## CAUTION (binding)
memo-ray is a deliberately-kept vendored fork (re-vendor discipline) and its embedder has wedged
before — emission must be ASYNC/QUEUED, never inline in ingest hot paths (the Marquez lesson).

## Context pointers
- Plan §9.5 M2-6 row (PLAN-local-power-unified-ui.md:535 — binding design intent)
- Sources: B0 knowledge/*.md format, R0 graph builders; sink: memoray/mcp-server
- A7 privacy partition applies to emitted content

## TDD plan
1. Red: a knowledge note lands → a queued triple emission appears; the ingest hot path's wall time is unchanged (±5%) with the sink down.
2. Red: queue drains when memo-ray returns; nothing lost, order preserved.
3. Implement async emitter + queue — green. 4. Triple schema validation.

## Acceptance
```gherkin
Feature: knowledge outlives the workspace
  Scenario: emission never blocks the hot path
    Given memo-ray is down
    When ingestion and knowledge writes proceed
    Then their wall time is unaffected and emissions queue durably

  Scenario: the queue is honest
    When memo-ray returns
    Then queued triples drain in order with zero loss and are searchable via the memo-ray MCP
```

## Out of scope
Re-vendoring memo-ray, embedder fixes, bidirectional sync.

## Handoff
Verifier re-runs m2-6.mjs with the sink up and down.
