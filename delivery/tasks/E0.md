---
id: E0
epic: EP-E
milestone: M1
okr: O4.KR4.3
deps: []
authority: human-signed
allowlist: [website/DESIGN-BRIEF.md, website/claims.json, scripts/gates/e0.mjs]
tools: [node]
sandbox: worktree
verify: node scripts/gates/e0.mjs
budget: 400
---

# E0 — Website design brief + claims registry (words locked before code)

## Goal
The brief locks every word: full copy deck (hero: "A local-first AI workbench. Your documents,
your models, your machine."), page structure, and checkable design constraints. claims.json is
the truth registry — every numeric/comparative claim must carry `claim`, `source`, `verified_date`.
The invalidated token-economics numbers are REMOVED, not reworded.

## Context pointers
- Plan §7 E0 card (PLAN-local-power-unified-ui.md:384-391); owner decisions locked at §7 head (honest voice, one demo moment, brief→mocks→contract)
- Keep from live site: earth-tone palette (unify with C0), Neuro-Assist dock, TUI accents, SEO/OG hygiene
- Memory context: memo-ray token-audit found "98%"/"-92.9%" claims NOT validated; v2 harness blocked — they do not qualify

## TDD plan
1. Write e0.mjs first: (a) brief exists with owner-approval line; (b) claims gate runs against the
   CURRENT website/ and FAILS — proving it catches the live violations. Red on (a), (b) must fail-as-designed.
2. Author brief + empty-but-schema'd claims.json. 3. Owner approves — green (claims gate stays armed for E2).

## Acceptance
```gherkin
Feature: structural honesty for the public face
  Scenario: the claims gate catches today's violations
    When the claims checker runs against the current website
    Then it exits non-zero listing every unregistered numeric claim

  Scenario: the brief is complete and approved
    Then DESIGN-BRIEF.md contains the full copy deck, wireframe order, and checkable constraints, with the owner's approval line
```

## Out of scope
Building anything (E1/E2), new claims (F5 is the future data source).

## Handoff
Owner signs the brief. E1 builds three mocks from it verbatim.
