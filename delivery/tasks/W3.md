---
id: W3
epic: EP-W
milestone: M1
okr: O2.KR2.3
deps: [W2]
authority: agent-ok
allowlist: [delivery/, scripts/gates/w3.mjs]
tools: [node]
sandbox: worktree
verify: node scripts/gates/w3.mjs
budget: 300
---

# W3 — Dogfood proof: a real phase delivered by the engine alone

## Goal

One full plan phase delivered end-to-end driven only by the engine: session starts with the
phase-runner skill + `work next`, never reads the plan file, completes the item, an independent
verifier confirms, and the ledger reconstructs the whole claim→verify→done chain. This is
KR2.3's direct instrument.

## Context pointers

- Plan §3.5 W3 card (PLAN-local-power-unified-ui.md:250-252); first candidates: any ready C or F item
- Ideally run once with Claude and once local-model-assisted (proves the token-frugal format fits small contexts)
- Frontier-consultation checkpoints list (plan line 254): contract authoring, blocker queue, VISION-CHECKs, milestone planning

## TDD plan

1. Author w3.mjs first (red): it audits a delivery transcript + ledger for the proof conditions.
2. Execute the dogfood delivery. 3. Gate goes green on the real evidence, not a fixture.

## Acceptance

```gherkin
Feature: the delivery engine delivers without the plan file
  Scenario: contract sufficiency is proven
    Given the executing session's transcript
    Then it contains no read of PLAN-local-power-unified-ui.md

  Scenario: the ledger tells the whole story
    Given the delivered phase's ledger events
    Then claim, verify handoff, and done reconstruct with distinct author and verifier ids
    And the delivered phase's own gate is green
```

## Out of scope

Delivering more than one phase this way (that becomes the default afterwards); local-model-only sessions (stretch evidence, not required for green).

## Handoff

Verifier re-runs w3.mjs over the recorded evidence. EP-W VISION-CHECK written (workstream closes).
