---
id: B3
epic: EP-B
milestone: M1
okr: O2.KR2.1
deps: [B1, C2]
authority: agent-ok
allowlist: [app/L0/_all/mod/_prime_silo/, tests/coordination/, scripts/gates/b3.mjs]
tools: [node, preview]
sandbox: worktree
verify: node scripts/gates/b3.mjs
budget: 400
---

# B3 — Bridge coordination panel (the board goes live)

## Goal
A live Bridge panel rendering the ledger: task board (todo/claimed/done, colored by agent),
knowledge feed, blocked items surfaced prominently; tasks carrying a `run_id` deep-link to the G2
live run view. This supersedes delivery/board/BOARD.md as the rendered view (files stay canonical
until the delivery board migrates to the ledger).

## Context pointers
- Plan §3 B3 card (PLAN-local-power-unified-ui.md:223-225)
- Uses the existing activity-store + EventBus SSE (coord.* topics from B1) — no polling, no second store
- C1 pane contract for layout; C0 tokens only (no hex literals)

## TDD plan
1. Red: preview-driven — create task via API → appears in panel without reload.
2. Red: block a task → blocked lane renders it prominently with its reason.
3. Implement panel — green. 4. Screenshots at 1280x800 and 1920x1080 archived.

## Acceptance
```gherkin
Feature: the shared board is visible where the human looks
  Scenario: live updates without reload
    Given the Bridge coordination panel is open
    When a task_created event is POSTed to the B1 API
    Then the task appears in the panel without a page reload

  Scenario: blocked work cannot hide
    Given a task_blocked event with a reason
    Then the panel surfaces it in the blocked lane with the reason visible
```

## Out of scope
Editing/claiming from the UI (read-first; verbs come via G2 authority levels), kanban drag-drop.

## Handoff
Verifier re-runs b3.mjs + inspects both screenshots. GUIDE.md Bridge section updated.
