---
id: F4
epic: EP-F
milestone: M1
okr: O3.works-with-you
deps: [G0, B1, C2]
authority: agent-ok
allowlist: [app/L0/_all/mod/_prime_silo/, server/, tests/, scripts/gates/f4.mjs]
tools: [node, preview]
sandbox: worktree
verify: node scripts/gates/f4.mjs
budget: 400
---

# F4 — "While you were away" recap

## Goal

On app open (and optional tray notification), ONE calm card folding the G0 stream + ledger since
last-seen: runs finished with one-line honest outcomes, tasks completed/blocked per agent, wedges/
failures, and things awaiting your signature at the top. One primary action per item. This
replaces polling — anything not in the recap didn't happen.

## Context pointers

- Plan §9.5 F4 card (PLAN-local-power-unified-ui.md:504-506)
- Sources: G0 events.jsonl folds + coordination ledger folds — no third store; last-seen timestamp per user
- C0 discipline: one primary action per item (open run / sign / dismiss)

## TDD plan

1. Red: finish a run + block a task while last-seen is rewound → recap shows exactly those two with correct outcomes.
2. Red: empty state shows nothing rather than filler.
3. Implement fold + card — green. 4. Dismissed items don't return.

## Acceptance

```gherkin
Feature: attention is spent once, on purpose
  Scenario: the recap is complete and exact
    Given a run finished and a task blocked while away
    When the app opens
    Then the recap shows exactly those two items with honest outcomes and signatures-needed on top

  Scenario: no filler
    Given nothing happened while away
    Then the recap renders its empty state with no manufactured content

  Scenario: dismissal is final
    Given an item is dismissed
    Then it does not reappear in later recaps
```

## Out of scope

Email/push notifications, digest scheduling (F3 can schedule it later), per-item chat.

## Handoff

Verifier re-runs f4.mjs. GUIDE.md Bridge section notes the recap contract.
