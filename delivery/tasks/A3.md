---
id: A3
epic: EP-A
milestone: M1
okr: O1.KR1.2
deps: [A1]
authority: agent-ok
allowlist: [manifests/offload/knowledge/, runtime/brain/, runtime/tests/brain/, scripts/gates/a3.py]
tools: [python, pytest, lemonade]
sandbox: worktree
verify: python scripts/gates/a3.py
budget: 500
---

# A3 — Knowledge-task manifest pack

## Goal
Four knowledge tasks as pypes DAG manifests: `summarize_doc` (windowed map-reduce), `card_gen`
(LONGVIEW card pattern generalized), `graph_enrich_batch` (wraps existing `benny enrich-graph`
stages), `report_section` (feeds Studio/LONGVIEW). Same design rules and gate pattern as A2.

## Context pointers
- Plan §2 A3 card (PLAN-local-power-unified-ui.md:160-163)
- Reuse-by-extraction rule: functions living in scripts/longview/ move into runtime/brain/ and are imported from both — never duplicated
- A2 design rules: deterministic pre-checks → judge → diff artifact; loops max 2

## TDD plan
1. summarize_doc fixture red → map-reduce green (assembled summary covers every section marker).
2. card_gen against a fixture session log → schema-valid card.
3. graph_enrich_batch wraps enrich stages without duplicating them (import assertion test).
4. report_section produces a citation-bearing section; pack gate a3.py runs all four.

## Acceptance
```gherkin
Feature: knowledge work offloads to the local model
  Scenario: every task passes its golden-input run
    Given each of the four manifests and its golden input
    When run against the real endpoint
    Then output is schema-valid with judge verdict recorded and diff artifact written

  Scenario: extraction not duplication
    Given a helper needed by both scripts/longview and a manifest handler
    Then it lives in runtime/brain/ and both import it (grep gate: no copied function bodies)
```

## Out of scope
New generation pipelines; Studio wiring (D-workstream); A7 card schema changes.

## Handoff
Verifier re-runs a3.py. Task catalog table updated alongside A2's.
