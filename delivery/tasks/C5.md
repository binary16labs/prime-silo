---
id: C5
epic: EP-C
milestone: M1
okr: O3.KR3.3
deps: [C0]
authority: agent-ok
allowlist: [app/L0/_all/mod/_prime_silo/, app/L0/_all/mod/_core/visual/, scripts/gates/c5.mjs]
tools: [node, preview]
sandbox: worktree
verify: node scripts/gates/c5.mjs
budget: 200
---

# C5 — Mascot micro-states (Benny idle/listening/processing)

## Goal

Benny gets CSS keyframe micro-states per the requirements doc — idle, listening, processing —
where the processing state reflects REAL activity (bound to G0/coord SSE events, not a timer).
`prefers-reduced-motion` honored throughout.

## Context pointers

- Plan §5 C5 card (PLAN-local-power-unified-ui.md:340-341); requirements: architecture/REQUIREMENTS-ui-ux-refactor.md (static mascot finding)
- C0 sanctioned-exception rule: mascot micro-states are the only ambient motion allowed
- Activity source: existing EventBus SSE (subscribe_all) — no polling

## TDD plan

1. Red: DOM test — mascot class changes to processing when an SSE activity event arrives, back to idle on quiet.
2. Red: with prefers-reduced-motion, zero running animations.
3. Implement keyframes + SSE binding — green.

## Acceptance

```gherkin
Feature: the mascot tells the truth
  Scenario: processing means processing
    Given the app is idle
    When a run event arrives on the SSE stream
    Then the mascot enters processing state, and returns to idle when the stream quiets

  Scenario: reduced motion is respected
    Given prefers-reduced-motion is set
    Then no mascot animation plays
```

## Out of scope

New mascot artwork, sound, chat behavior (Benny pilot owns interaction).

## Handoff

Verifier re-runs c5.mjs. Small task — pairs well as a first W3 dogfood candidate.
