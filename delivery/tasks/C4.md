---
id: C4
epic: EP-C
milestone: M1
okr: O3.KR3.1
deps: [C2]
authority: agent-ok
allowlist:
  [
    app/,
    server/pages/,
    packaging/,
    GUIDE.md,
    README.md,
    QUICKSTART-EXE.md,
    scripts/gates/c4.mjs,
    scripts/debrand/
  ]
tools: [node]
sandbox: worktree
verify: node scripts/gates/c4.mjs
budget: 600
---

# C4 — De-brand pass (user-facing strings ONLY)

## Goal

Zero "Space Agent" anywhere a user can see: string literals, titles, alt text, logos, docs prose —
from the ~91-occurrence audit. Identifiers are explicitly forbidden in this phase.

## FORBIDDEN (Workstream H, alias-first, later)

`space-desktop:` IPC channels, `node space` CLI name, `space.js`/module file names,
`package.json` name fields, upstream space-agent dependency metadata.

## Context pointers

- Plan §5 C4 card (PLAN-local-power-unified-ui.md:335-338); audit grep from §0 fact row (91 places / 40+ files)
- Steps: generate the authoritative hit list into scripts/debrand/audit.md, classify each hit user-facing|identifier|vendored, replace only user-facing, check off every line

## TDD plan

1. Red: c4.mjs — case-insensitive grep for space agent|space-agent over rendered UI strings, HTML, docs (minus the identifier/vendored allowlist file) is non-zero today.
2. Classify + replace — green. 3. App boots; IPC smoke test passes (proves identifiers untouched).

## Acceptance

```gherkin
Feature: the product has one name
  Scenario: no user-facing legacy brand remains
    When the debrand grep runs over UI strings, HTML, and docs excluding the identifier allowlist
    Then it returns zero hits

  Scenario: identifiers were not harmed
    When the app boots and the IPC smoke test runs
    Then space-desktop channels and the node space CLI work unchanged
```

## Out of scope

Identifier renames (H), website (E owns its own copy), vendored memoray strings.

## Handoff

Verifier re-runs c4.mjs + boots the app. The classified audit file is the review artifact.
