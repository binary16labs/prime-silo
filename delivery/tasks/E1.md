---
id: E1
epic: EP-E
milestone: M1
okr: O4.KR4.3
deps: [E0, C2]
authority: human-signed
allowlist: [website/prototypes/, scripts/gates/e1.mjs]
tools: [node, preview]
sandbox: worktree
verify: node scripts/gates/e1.mjs
budget: 600
---

# E1 — Three static mocks, owner picks one (second human gate)

## Goal

Three fully responsive static variants of the same E0 brief (e.g. editorial-calm /
product-shot-forward / TUI-flavored), HTML/CSS only, shared C0 token file, screenshotted at
390/1280/1920. The owner picks one; its screenshots become the E2 acceptance contract.

## Context pointers

- Plan §7 E1 card (PLAN-local-power-unified-ui.md:393-396)
- Real shell screenshots come from C2 (hence the dep); demo moment is a placeholder only
- No JS beyond the demo placeholder; no CDN assets (self-host per Q1 supply-chain rules)

## TDD plan

1. Red: e1.mjs — all three mocks pass the E0 claims gate + C0 token lint + reduced-motion check.
2. Build mocks — green. 3. Screenshot all three at all three widths. 4. Owner pick recorded in the brief — gate asserts the pick line.

## Acceptance

```gherkin
Feature: the owner chooses from honest options
  Scenario: every mock is compliant before choice
    Given the three prototype variants
    Then each passes the claims gate, uses only C0 tokens, and honors reduced motion at 390, 1280, and 1920

  Scenario: the pick is the contract
    When the owner records their pick in the brief
    Then the chosen mock's screenshots are archived as the E2 acceptance contract
```

## Out of scope

The production build (E2), copy changes (brief is locked; changes = E0 rev).

## Handoff

Owner picks. E2 implements the winner pixel-faithfully.
